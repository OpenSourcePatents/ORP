<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
# Session addendum — 2026-06-11 — branch `app-core`

Branch `app-core` off `master` @ 44d24fa. Full pytest green at every commit
(191 → 253 tests). Rules honored throughout: no Co-Authored-By trailers;
explicit-path `git add` only; SPDX header on every new `.py`; one commit per task;
the forward-only wall held everywhere — bank schedules are inputs, crossrange is an
output, nothing introduced accepts a target endpoint and produces controls (now
enforced mechanically at the CLI seam by the UI-wall parser-tree test).

## Commits

| Commit | Task | Content | Tests |
|---|---|---|---|
| 711f428 | 0 | Tip fixes: author email → opensourcepatents@gmail.com; absolute local paths removed from 4 tracked files (3 named + 1 found by sweep); `git grep` sweep clean | 191 |
| d292fa2 | 1 | `BankSchedule.from_csv` (strict two-column loader, refuses rather than repairs, convention detect/normalize, mandatory provenance) + 34 tests | 225 |
| bbf7938 | 1b | Gate-3 propagation bit-identity test: `run_replay` executed for both loading paths, full state history + every derived channel bit-identical (result: identical) | 226 |
| 24ecbff | 2 | Reproducible-run sessions: `save_session`/`load_session`, SHA-256 vehicle + schedule-content hashes, refusal-over-repair load, save-is-pure-recording, schema wall test | 238 |
| 4a7f922 | 2b | `load_session` refuses `frame: inertial` explicitly with convert-first-then-save guidance (distinct from the generic unknown-frame error) | 239 |
| 70f6ab9 | 3 | ORP CLI: `orp run` / `orp vehicles` / `orp gates`; console entry point; UI-wall test; one-line refusals; frame conversion at the CLI boundary | 253 |
| 17f0972 | 4 | Display-only heading wrap in the CLI summary; README Quickstart (corrected stdout, weakest-link rationale, from_csv format with the Artemis worked example + verbatim tag/caveat, session reproduction limitation) | 253 |
| (this) | 4 | Session addendum | 253 |

## Divergences (as reported per task)

**Task 0** — the path sweep found a fourth file beyond the three named
(`docs/sources/msl_aas13-307_karlgaard_meads.md`); fixed via the already-cited NTRS
accession.

**Task 1** (mid-tier subagent; two divergences it failed to self-report, caught on
verification) —
1. The tests never load `data/flights/artemis1_bank_commanded.csv` through `from_csv`:
   that file is four-column with deliberately blank flagged cells, which the specified
   strict loader must refuse — an internal conflict in the task prompt. Resolved with
   temporary two-column extracts (hold-last + reversal-timed); all six Table-3
   reversals land within 1 s (max 0.975 s).
2. The original "Gate 3 replay identity" test compared schedules, not trajectories.
   Closed by the Task-1b follow-up: the actual `run_replay` propagation, both loading
   paths, bit-identical across all 16 channels and the full derived report
   (tolerance-free; monkeypatched loader, gate modules untouched; the docstring
   addition is two substantive sentences plus a four-word bold lead-in).

**Task 2** (large-tier subagent; reported NONE, three judgment calls surfaced on
verification) —
1. The session frame tag is hardcoded `planet-relative` on save (SimulationConditions
   has no frame field; ORP's entry state is inherently planet-relative).
2. `frame: inertial` was initially accepted-but-ignored on load; closed by 4a7f922
   (explicit refusal with convert-first guidance).
3. Unknown vehicle/planet raise native `FileNotFoundError`/`KeyError` (not
   `SessionError` subclasses); a missing schedule-provenance block falls back to
   `NOT_VALIDATED` — kept by instruction as the honest floor tag, not a repair
   (content reproducibility is hash-guarded).

**Task 3** —
1. The replay gate exposes no status constant; the `orp gates` line composes the gate
   report's verbatim verdict fragments ("FAIL against the pre-registered tolerances",
   "NOT LOCKED", "stays NOT_VALIDATED").
2. trajectory.csv headers state each channel's recorded unit (deg, g, dimensionless
   Mach) — not strictly SI for those.
3. The frame-equivalence test fixes the inertial expression and derives the relative
   expression via the real forward conversion (ULP-matched degree strings make CLI
   parsing bit-exact); the inverse conversion is test-local and used as a cross-check —
   `orp.core.frames` is deliberately forward-only, and an inverse-first construction
   cannot be bit-exact through float trig.
4. `--bank-deg` constants get NOT_VALIDATED provenance (hand-entered, unsourced); the
   prompt prescribed ASSERTED only for CSV.
5. Entry-state flags default to `SimulationConditions`' own field defaults (read
   live); only `--frame`/`--out`/`--vehicle`/`--planet` are required.
6. Aero model (unspecified): `ConstantCoefficientCalculator` from the vehicle's cited
   CD and L/D at their weakest-link provenance (repo gate/plot convention).
7. Adversarial-review fixes folded in: one-line parse errors (ArgumentParser.error
   override), finiteness/positivity validation (`--dt 0` previously looped forever),
   malformed-vehicle-YAML refusal, absolute `--bank-csv` path recorded in
   session.yaml, wall test scans rendered help + group titles, e2e provenance
   assertion compares against the level the engine literally stamps on a rerun, gates
   nonzero-exit path tested.

**Task 4 (this phase)** — heading wrap is display-only (`% 360` in the summary
printer); the recorded channel and trajectory.csv are byte-for-byte unchanged. README
gained only the Quickstart section; no validation language was strengthened anywhere
(the Quickstart itself shows a NOT_VALIDATED run and says why).

## Open items

1. **Session schema gap (documented limitation, now also in README):** the session
   format records no aerodynamic-calculator choice, so `load_session` + simulate
   rebuilds with the engine default and does not reproduce a CLI trajectory (the CLI
   uses constant vehicle coefficients). Closing it needs a schema field + format
   version bump in `orp/core/session.py`.
2. **Gates data path:** `gate3_artemis_replay.SCHEDULE_CSV` resolves to repo-root
   `data/flights/`, outside the installable package — `orp gates` requires a repo
   checkout. Pre-existing gate-module behavior, named not patched.
3. **`orp.core.frames` has no inverse** (deliberate); the test-local inverse in
   `test_cli.py` is the only relative→inertial expression in the tree.
4. **`test_smoke.py:ALL_MODULES` is hand-curated** and now trails the package
   (`orp.core.session`, `orp.gates.gate3_artemis_replay`, `orp.cli` absent) — left
   untouched per task scope.
5. Apollo Earth e2e ends at altitude −5.5 m (GROUND_HIT overshoots within one step) —
   engine termination behavior, recorded faithfully.
6. Branch not merged/pushed as of this commit (merge to master is the next phase).
